<% provide(:title, 'All users') %>
<h1>All users</h1>
<div class="row">
  <div class="span6 offset3">
 <%= form_for :user do |f| %>
<%= f.label :search %>
<%= f.text_field :search %>
<%= f.submit "Search", class: "btn btn-large btn-primary" %>
<%= will_paginate %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>

<%end%>
 </div>
</div>
